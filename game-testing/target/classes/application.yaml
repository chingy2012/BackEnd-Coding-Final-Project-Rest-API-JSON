spring:
   datasource:
      username: game_testing
      password: game_testing
      url: jdbc:mysql://localhost:3306/game_testing
      
   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true
      defer-datasource-initialization: true
   
   sql:
      init:
         mode: always
         #never